import { SolutionValidationErrorPipe } from './solution-validation-error.pipe';

describe('SolutionValidationErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new SolutionValidationErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
