import { ApiAuthorizationTypePipe } from './api-authorization-type.pipe';

describe('ApiAuthorizationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiAuthorizationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
