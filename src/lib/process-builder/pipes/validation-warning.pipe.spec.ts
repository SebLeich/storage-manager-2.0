import { ValidationWarningPipe } from './validation-warning.pipe';

describe('ValidationWarningPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidationWarningPipe();
    expect(pipe).toBeTruthy();
  });
});
