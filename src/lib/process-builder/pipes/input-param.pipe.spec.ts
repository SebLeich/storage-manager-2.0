import { InputParamPipe } from './input-param.pipe';

describe('InputParamPipe', () => {
  it('create an instance', () => {
    const pipe = new InputParamPipe();
    expect(pipe).toBeTruthy();
  });
});
