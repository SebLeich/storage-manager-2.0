import { TaskCreationStepPipe } from './task-creation-step.pipe';

describe('TaskCreationStepPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskCreationStepPipe();
    expect(pipe).toBeTruthy();
  });
});
