import { ReturnValueStatusPipe } from './return-value-status.pipe';

describe('ReturnValueStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ReturnValueStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
