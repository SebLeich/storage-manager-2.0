import { SortedFormArrayPipe } from './sorted-form-array.pipe';

describe('SortedFormArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortedFormArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
